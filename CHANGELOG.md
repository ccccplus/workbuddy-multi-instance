# 更新日志 · Changelog

本指南的迭代历史。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循语义化版本：**小版本** = 文档/脚本增强，**大版本** = 方案结构性变化（如官方改版导致补丁点失效）。

---

## [1.2.0] — 2026-09-20 · 新增 Windows 可行性探讨

回答一个被问到的问题：**这套方案能不能搬到 Windows 上？**

### 📝 文档 · 新增

- **`docs/windows.md`**（未在 Windows 实测，全文区分「实证 / 推演 / 未知」）
  - **三条实证**（均来自 macOS 5.5.6 程序包，可复现）：
    1. 读 `WORKBUDDY_CONFIG_DIR` 的是纯 Node 代码 → 平台无关
    2. `app.setPath('userData', '<数据目录>/app')` 主动重定向，官方注释点名 `%APPDATA%` 不被使用
    3. product.json 里有完整 Windows 字段集：`win32x64AppId` / `win32arm64AppId` /
       `win32x64UserAppId` / `win32arm64UserAppId` / `win32MutexName` / `win32AppUserModelId`
  - **结论**：思路通用、脚本不通 —— 数据目录与登录态两层可直接搬，
    身份层要换成 Windows 字段，且 Windows **不需要重签名**（比 macOS 省心）
  - **推演路线**：复制目录 → 改 product.json（含字段对照表）→ `.bat` 注入环境变量 → 6 条验证清单
  - **三个真未知**：`win32MutexName` 是否由原生启动器强制（最大不确定项）、
    安装版 vs 解压版差异、Windows 包结构是否同构

### 🔧 文档 · 修正

- README FAQ「Windows 能用吗」：原文「不适用」过于绝对且无依据 →
  改为「思路通用但脚本不通、未实测」，并指向 `docs/windows.md`

---

## [1.1.0] — 2026-09-20 · 同步 5.5.6 实战经验

**背景**：主 app 自动更新到 5.5.6 后，把停在 5.5.2 的副本手动同步上来。
全程数据目录不动、登录态自动保留（无需重新登录）。这次实战暴露了几处过时表述，
一并修正并把经验沉淀进脚本。

### 📝 文档 · 新增

- **README「升级与回退」章节重写**
  - 说明副本**为何不参与**官方自动更新：官方更新器会用原版包覆盖三件套补丁，隔离会当场失效
  - 新增「怎么知道该不该升」：跑一次脚本即打印两边版本号，相同则跳过
  - 补上读者最关心的一条：**跨版本升级后无需重新登录**（副本启动后自动刷新自己的会话文件）
  - 补「升级后建议跑一次全量自检」
- **README「已知无害告警」**新增 `SandboxCenter lock-conflict retry`
  —— 升级重启瞬间旧进程还没放手，重试几次就拿到锁，不是故障
- **troubleshooting「验证状态速记」**新增「跨版本升级后登录态保留 ✅ 实测」

### 🔧 文档 · 修正

- 升级耗时「约 10 秒」→ **约 1 分钟**（`cp` 1 GB + `codesign --deep` 的实测耗时）
- 版本标注补 **5.5.6**：`5.5.2 / 5.5.3` → `5.5.2 / 5.5.3 / 5.5.6`
- `BundleMigration` 缓存目录相关两处表述加时代注记：**5.5.6 起官方疑似不再生成该目录**，
  磁盘占用那节同步说明「查无此目录属正常，不必找」
- **troubleshooting 坑 3.1 补注**：该目录虽不再生成，但 `darwinBundleIdentifier`
  **仍必须改**（它还决定偏好设置 plist 等其它路径）

### ⚙️ 脚本 · 增强

- **`setup-workbuddy-2.sh`**（三个增强）
  - 版本相同则**自动跳过**，不再白白关掉副本重建一遍（`--force` 可强制重建）
  - 部署后跑**三件套自检**，任一不对立刻停手报警 —— 与其事后排查三天，不如当场发现
  - 提示「插件补丁可能被插件自身升级冲掉」，升级后需重打
  - 注释记录 macOS bash 3.2 把中文标点吞进变量名的坑（须写 `${VAR}`）
- **`verify-isolation.sh`**
  - 第 5 项判据修正：主实例也无 `BundleMigration` 目录时判「该版本不适用，非故障」，消除误报

### ✅ 兼容性确认

5.5.6 的三处补丁点（`productName` / `darwinBundleIdentifier` / `authentication.id`）
**依然在原位**，升级脚本无需改写。

---

## [1.0.0] — 2026-09-08 · 初版发布

- **完整方案**：官方环境变量 `WORKBUDDY_CONFIG_DIR` + `product.json` 三件套改写，零破解、随时可回退
- **README.md**：原理、隔离地图、6 步手动流程、踩坑速查表、FAQ
- **AGENT.md**：给 AI 操作员的一键部署手册（铁律 / 阶段验收 / 回滚）
- **scripts/**：`setup-workbuddy-2.sh`（部署/修复/升级三合一）、`verify-isolation.sh`（6 组隔离自检）
- **docs/**：`troubleshooting.md`（四幕踩坑全记录）、`isolation-map.svg`（双泳道隔离地图）
- **llms.txt**：面向 LLM 的索引文件

关键发现（本方案的灵魂）：

1. 官方在 `product.json` 里留了三处身份开关，其中 `authentication.id` 决定共享 auth 目录里的
   **会话文件名** —— 两实例同名才是「同登同退」的真凶（不是服务器绑定）
2. `CFBundleName` **绝不能改** —— Electron 按它找 Helper app，改了就是「Dock 有图标但永远没窗口」
3. 第三方插件可能硬编码 `~/.workbuddy`，无视环境变量 —— 需要单独打路径补丁

---

## 已知边界（不会修，属方案固有）

- 两个实例打开**同一个工作文件夹**时操作的是同一批文件（物理规律，无法隔离）
- Windows 版有官方单实例锁与不同目录结构，本方案不适用
- 服务端风控无法从客户端代码判断，实测未触发，风险自担
